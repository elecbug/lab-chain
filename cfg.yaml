log_level: "info"
mode: "full" # full, light, boot
network:
  ip_address: "0.0.0.0"
  max_peers: 50
dht:
  mode: "server"
  bootstrap_peers:
    - "peer1.example.com:1234"
    - "peer2.example.com:5678"
